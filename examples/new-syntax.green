(include "stdio.h" "stdlib.h")

(var xs (* I32) ())
(var (x y z) (* I32) ())

;(macro drain (var-name) (return (quote            
    (for () (> (unquote var-name) 0) (-- (unquote var-name)) 
        (++ z)) 
)))

(fun + (x I32 y I32) -> I32
    (var z I32)
    (drain x)
    (drain y)
    (return z))

(fun forever () 
    (for () () ()))

(fun main ()
    (printf "asdf !!"))

(fun calculate-infinity () -> I32
    (var x I32)
    (for () () ()
        (++ x))
    (return x))